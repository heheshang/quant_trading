# 技术设计文档

> **适用于**：阶段 3（编码前）
> **负责角色**：Owner Agent（编写）→ Evaluator Agent（评审）

## 设计概述

| 字段 | 内容 |
|------|------|
| 需求来源 | {关联 spec.md 标题或 Issue} |
| 设计者 | {角色} |
| 评审者 | {角色} |
| 审核人 | {角色} |
| 状态 | {draft / reviewing / approved / superseded} |

## 变更范围

```
{一句话概括变更范围}
```

### 新增文件
- `{路径}`: {用途}

### 修改文件
- `{路径}`: {修改要点}

### 删除文件
- `{路径}`: {删除理由}

## 架构决策

> 记录关键的设计选择，建议每个决策附带上下文和替代方案。

### ADR-{N}: {决策标题}

- **上下文**：{为什么需要做这个决策}
- **方案**：{选择了什么方案}
- **替代方案**：{备选方案及未选理由}
- **影响**：{对系统的影响}

## 数据流

```
{ASCII flow 或 mermaid 序列图描述核心数据流}
```

### 入参/出参

| 端点/接口 | 输入 | 输出 | 说明 |
|-----------|------|------|------|
| {接口名} | {参数} | {返回值} | {说明} |

## 安全考虑

- [ ] 输入校验（禁止信任上游输入）
- [ ] 权限检查（是否已鉴权）
- [ ] SQL 注入防护（使用参数化查询）
- [ ] 敏感数据脱敏（日志/响应中）
- [ ] 限流/熔断保护
- [ ] 无接口暴露内部实现细节

## 测试策略

| 类型 | 范围 | 策略 |
|------|------|------|
| 单元测试 | {模块} | {测试策略} |
| 集成测试 | {模块} | {测试策略} |
| 端到端测试 | {场景} | {测试策略} |

## 兼容性

- **API 兼容性**：{向后兼容/需版本升级/需迁移}
- **数据库兼容性**：{是否需要 migration / 数据修复}
- **配置兼容性**：{配置项变更说明}

## 回滚方案

```bash
# 回滚步骤（示例）
git revert <commit-hash>
# 数据回滚命令（如有）
```

## 附录

- {参考文档或相关 Issue 链接}
