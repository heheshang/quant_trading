# 测试计划与报告

> **适用阶段**：阶段 5（测试）
> **生成者**：实现 Agent → Evaluator Agent 审核

## 测试范围

| 维度 | 说明 |
|------|------|
| 变更标识 | {关联目录名} |
| 测试策略 | {unit / integration / e2e / manual} |
| 专项测试 | {性能/安全/兼容性/无} |

## 单元测试

### 测试用例清单

| # | 测试类 | 方法 | 场景 | 输入 | 预期输出 | 状态 |
|---|--------|------|------|------|----------|------|
| 1 | `{TestClass}` | `{testMethod}` | {场景描述} | {输入参数} | {预期结果} | {pass/fail} |
| 2 | `{TestClass}` | `{testMethod}` | {场景描述} | {输入参数} | {预期结果} | {pass/fail} |

### 边界覆盖

- [ ] 正常输入 → 正常输出
- [ ] 空值/null 输入 → 优雅降级或明确异常
- [ ] 超长输入 → 截断或拒绝
- [ ] 并发调用 → 无竞态

## 集成测试

| # | 场景 | 步骤 | 预期结果 | 状态 |
|---|------|------|----------|------|
| 1 | {场景} | {步骤列表} | {预期} | {pass/fail} |
| 2 | {场景} | {步骤列表} | {预期} | {pass/fail} |

## 质量门禁结果

### QG-1 编译检查
- **结果**：{pass / fail}
- **编译命令**：`{command}`
- **日志摘要**：
```
{关键日志输出}
```

### QG-2 类型检查
- **结果**：{pass / fail}
- **命令**：`{command}`
- **统计**：{errors} errors, {warnings} warnings

### QG-3 代码风格
- **结果**：{pass / fail}
- **命令**：`{command}`
- **报告文件**：`{路径}`

### QG-4 复杂度检查
- **结果**：{pass / fail}
- **最复杂文件**：`{文件}`, {cyclomatic complexity} → {建议}

## 覆盖率报告

| 指标 | 当前值 | 目标值 | 状态 |
|------|--------|--------|------|
| 行覆盖率 | {N}% | ≥{N}% | {pass/fail} |
| 分支覆盖率 | {N}% | ≥{N}% | {pass/fail} |

## 已知问题

| # | 问题 | 影响 | 计划修复时间 |
|---|------|------|-------------|
| 1 | {问题描述} | {低/中/高} | {时间} |

## 测试结论

```
{测试通过 / 有条件通过 / 不通过，及总体质量评估}
```
