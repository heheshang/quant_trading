# 量化交易系统开发指南

## 1. 项目概述

本系统是一个专业的量化交易软件系统，基于 **Rust + Tauri 2.0 + Vue3 + PostgreSQL + Redis** 技术栈构建。

### 1.1 核心特性

- ✅ **模块化架构**：高内聚低耦合的五大核心模块
- ✅ **多数据库支持**：PostgreSQL (关系型，含历史行情分区表) + Redis (缓存)
- ✅ **完整回测系统**：支持策略开发、参数优化、性能评估
- ✅ **智能执行算法**：TWAP、VWAP、冰山订单等
- ✅ **三层风控体系**：事前、事中、事后全流程风险管理
- ✅ **实时监控告警**：Prometheus 指标 + 多渠道告警
- ✅ **现代化界面**：Vue3 + Element Plus + ECharts

### 1.2 系统架构

```
quant-trading-system/
├── src-tauri/                 # Tauri 后端（Rust）
│   └── src/
│       ├── main.rs            # 主入口
│       ├── commands/          # Tauri 命令（分文件：twofa/audit/optimizer 等）
│       └── state.rs           # 应用状态
├── crates/                    # Rust 模块
│   ├── common/                # 公共模块（类型、错误、工具）
│   ├── data-layer/            # 数据管理层
│   ├── strategy-layer/        # 策略开发层
│   ├── trading-layer/         # 交易执行层
│   ├── risk-layer/            # 风险管理层
│   └── monitor-layer/         # 监控告警层
├── src/                       # Vue3 前端
│   ├── views/                 # 视图页面
│   ├── router/                # 路由配置
│   └── main.ts                # 前端入口
├── Cargo.toml                 # Workspace 配置
├── package.json               # 前端依赖
└── .env.example               # 环境变量模板
```

## 2. 开发环境配置

### 2.1 环境要求

- **Rust**: 1.77+
- **Node.js**: 18+
- **PostgreSQL**: 14+
- **Redis**: 6+
- **历史行情**: 存储于 PostgreSQL 分区表（不使用 InfluxDB）

### 2.2 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd ea_test
```

2. **配置环境变量**
```bash
cp .env.example .env
# 编辑 .env 文件，配置数据库连接、币安密钥等
```

3. **用 Docker 启动数据库组件（推荐）**

   系统配置由 `dotenv` 从 `.env` 注入；本地开发可用 Docker 提供 PostgreSQL + Redis：
```bash
# 启动 compose.yaml 中的 postgres + redis（映射到 127.0.0.1:15432 / 16379）
docker compose up -d postgres redis
```

   在 `.env` 中指向 Docker 端口（默认值已写入 `.env.example` 下方，需自行确认）：
```dotenv
DATABASE_HOST=127.0.0.1
DATABASE_PORT=15432
DATABASE_USERNAME=quant
DATABASE_PASSWORD=quant_password
DATABASE_NAME=quant_trading
REDIS_HOST=127.0.0.1
REDIS_PORT=16379
```

   运行迁移（幂等，可重复执行）：
```bash
cd src-tauri && DATABASE_HOST=127.0.0.1 DATABASE_PORT=15432 \
  DATABASE_USERNAME=quant DATABASE_PASSWORD=quant_password \
  DATABASE_NAME=quant_trading cargo run --bin migrate-db up
```

4. **安装前端依赖**
```bash
npm install
```

5. **运行开发环境**
```bash
# 方式1：同时启动前后端（自动后台迁移）
npm run tauri dev

# 方式2：分别启动
npm run dev          # 启动前端
cargo tauri dev      # 启动 Tauri
```

6. **生产构建**
```bash
npm install
```

4. **运行开发环境**
```bash
# 方式1：同时启动前后端
npm run tauri dev

# 方式2：分别启动
npm run dev          # 启动前端
cargo tauri dev      # 启动 Tauri
```

5. **生产构建**
```bash
npm run build
npm run tauri build
```

## 3. 核心模块说明

### 3.1 数据管理模块 (data-layer)

- **PostgreSQL**: 存储订单、持仓、账户等关系型数据
- **Redis**: 缓存热点数据，降低延迟
- **历史行情**: 存储于 PostgreSQL 分区表（不使用 InfluxDB）
- **数据质量**：实时清洗、去重、异常检测

### 3.2 策略开发模块 (strategy-layer)

- **策略接口**：统一的策略开发框架
- **技术指标**：SMA、EMA、RSI、MACD、布林带等
- **回测引擎**：高保真回测，包含滑点、手续费模拟
- **性能指标**：夏普比率、最大回撤、胜率等

### 3.3 交易执行模块 (trading-layer)

- **订单管理**：订单生命周期管理
- **执行引擎**：支持模拟盘和实盘切换
- **算法交易**：TWAP、VWAP、冰山订单
- **延迟监控**：从信号到成交的全链路延迟统计

### 3.4 风险管理模块 (risk-layer)

- **事前风控**：资金检查、持仓限制、集中度控制
- **实时监控**：账户风险、保证金、回撤监控
- **事后分析**：VaR 计算、压力测试、归因分析

### 3.5 监控告警模块 (monitor-layer)

- **Prometheus 指标**：订单量、延迟、账户余额等
- **结构化日志**：基于 tracing 的分级日志系统
- **多渠道告警**：邮件、Webhook、企业微信

## 4. 开发规范

### 4.1 代码规范

#### 4.1.1 Rust 代码规范

1. **命名规范**
   - 模块和 crate 使用 kebab-case 命名
   - 结构体使用 PascalCase 命名
   - 函数和变量使用 snake_case 命名
   - 常量使用 UPPER_SNAKE_CASE 命名

2. **错误处理**
   - 使用 `anyhow` 和 `thiserror` 进行错误处理
   - 自定义错误类型应实现 `std::error::Error` trait
   - 错误信息应清晰描述问题和解决方案

3. **异步编程**
   - 使用 `tokio` 作为异步运行时
   - 避免阻塞操作，使用异步替代方案
   - 合理使用 `async`/`await`，避免过度并发

4. **日志记录**
   - 使用 `tracing` 进行结构化日志记录
   - 日志级别应合理使用（trace, debug, info, warn, error）
   - 敏感信息不应记录到日志中

#### 4.1.2 TypeScript 代码规范

1. **命名规范**
   - 组件文件使用 PascalCase 命名
   - 工具函数使用 camelCase 命名
   - 常量使用 UPPER_SNAKE_CASE 命名

2. **组件开发**
   - 使用 Composition API (script setup)
   - 组件应具有单一职责
   - 合理使用响应式数据和计算属性

3. **类型安全**
   - 所有变量应有明确类型定义
   - 使用 TypeScript 接口定义复杂数据结构
   - 避免使用 any 类型

### 4.2 数据库规范

#### 4.2.1 PostgreSQL 规范

1. **表设计**
   - 表名使用 snake_case 命名
   - 字段名使用 snake_case 命名
   - 主键统一命名为 `id`
   - 时间戳字段使用 `created_at` 和 `updated_at`

2. **索引设计**
   - 为经常查询的字段创建索引
   - 复合索引应考虑字段顺序
   - 定期检查和优化索引性能

3. **迁移脚本**
   - 使用 SQLx 进行数据库迁移
   - 迁移文件命名格式：`{timestamp}_{description}.sql`
   - 迁移应具有幂等性

#### 4.2.2 Redis 规范

1. **键命名**
   - 使用冒号分隔命名空间：`app:module:key`
   - 键名应具有描述性但不过长
   - 避免使用特殊字符

2. **数据结构选择**
   - 根据使用场景选择合适的数据结构
   - 合理设置过期时间
   - 避免存储过大的数据

### 4.3 API 规范

#### 4.3.1 Tauri 命令规范

1. **命名规范**
   - 命令名使用 snake_case 命名
   - 命令应具有描述性名称
   - 相关命令应使用统一前缀

2. **参数设计**
   - 参数应有明确的类型定义
   - 复杂参数应使用结构体封装
   - 可选参数使用 Option 类型

3. **返回值设计**
   - 成功时返回具体数据
   - 失败时返回错误信息
   - 避免返回过于复杂的数据结构

#### 4.3.2 前端 API 调用规范

1. **错误处理**
   - 所有 API 调用应有错误处理
   - 用户友好的错误提示
   - 必要时记录错误日志

2. **加载状态**
   - 异步操作应有加载状态提示
   - 合理使用骨架屏或加载动画
   - 防止重复提交

### 4.4 安全规范

#### 4.4.1 数据安全

1. **敏感数据加密**
   - 密码使用 Argon2 进行哈希处理
   - API 密钥等敏感信息使用 AES-256-GCM 加密
   - 加密密钥不应硬编码在代码中

2. **访问控制**
   - 实现基于角色的访问控制（RBAC）
   - 敏感操作需要权限验证
   - 定期审查权限分配

#### 4.4.2 通信安全

1. **API 安全**
   - 使用 JWT 进行身份验证
   - 实现请求签名防止篡改
   - 设置合理的请求频率限制

2. **网络安全**
   - 使用 HTTPS 进行数据传输
   - 实现 IP 白名单机制
   - 定期更新依赖库修复安全漏洞

## 5. 测试规范

### 5.1 单元测试

1. **覆盖率要求**
   - 核心业务逻辑测试覆盖率应达到 80% 以上
   - 工具函数应有 100% 测试覆盖率
   - 错误处理路径应有对应测试

2. **测试编写**
   - 使用 Rust 的内置测试框架
   - 测试应具有独立性，不依赖外部状态
   - 测试数据应具有代表性

### 5.2 集成测试

1. **数据库测试**
   - 测试数据库连接和基本操作
   - 验证迁移脚本的正确性
   - 测试事务处理的正确性

2. **API 测试**
   - 测试 Tauri 命令的输入输出
   - 验证错误处理的正确性
   - 测试边界条件和异常情况

## 6. 部署规范

### 6.1 环境配置

1. **生产环境变量**
   - 所有敏感信息应通过环境变量配置
   - 环境变量应有合理的默认值
   - 配置应有文档说明

2. **日志配置**
   - 生产环境应使用文件日志
   - 日志应按日期分割
   - 合理设置日志级别

### 6.2 监控告警

1. **指标收集**
   - 收集关键业务指标
   - 监控系统性能指标
   - 设置合理的告警阈值

2. **告警机制**
   - 实现多渠道告警
   - 告警应有去重机制
   - 告警信息应包含足够的上下文

## 7. 性能优化

### 7.1 数据库优化

1. **查询优化**
   - 避免 N+1 查询问题
   - 合理使用分页查询
   - 复杂查询应有执行计划分析

2. **连接池管理**
   - 合理设置连接池大小
   - 及时释放数据库连接
   - 监控连接池使用情况

### 7.2 缓存优化

1. **Redis 缓存**
   - 合理设置缓存过期时间
   - 缓存热点数据
   - 实现缓存更新策略

2. **内存缓存**
   - 使用合适的数据结构
   - 控制缓存大小
   - 实现缓存淘汰机制

### 7.3 前端优化

1. **打包优化**
   - 启用代码分割
   - 压缩静态资源
   - 合理使用懒加载

2. **渲染优化**
   - 虚拟滚动处理大量数据
   - 合理使用 keep-alive
   - 避免不必要的重渲染

## 8. 故障处理

### 8.1 常见问题

1. **编译问题**
   - 检查 Rust 版本是否符合要求
   - 确认依赖库版本兼容性
   - 清理构建缓存重新编译

2. **运行时问题**
   - 检查环境变量配置
   - 确认数据库连接正常
   - 查看日志定位问题

### 8.2 应急预案

1. **数据恢复**
   - 定期备份关键数据
   - 实现数据恢复流程
   - 测试备份数据有效性

2. **服务降级**
   - 实现核心功能降级方案
   - 缓存关键数据
   - 提供离线模式

## 9. 版本管理

### 9.1 分支策略

1. **主分支**
   - `main` 分支为生产环境代码
   - 代码合并前应通过所有测试
   - 发布版本应打标签

2. **开发分支**
   - `develop` 分支为开发环境代码
   - 功能开发应在特性分支进行
   - 特性分支命名：`feature/功能名称`

### 9.2 发布流程

1. **版本号规范**
   - 遵循语义化版本号规范
   - 主版本号：不兼容的API修改
   - 次版本号：向下兼容的功能性新增
   - 修订号：向下兼容的问题修正

2. **发布检查**
   - 确认所有测试通过
   - 更新版本号和变更日志
   - 打标签并推送至远程仓库